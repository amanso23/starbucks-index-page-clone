---

interface Props{
    bannerImg: string
}

const { bannerImg } = Astro.props

---

<picture class="lg:sticky lg:top-0 w-full lg:h-screen">
    <img src={bannerImg} alt="banner-img" class="w-full h-screen">
</picture>